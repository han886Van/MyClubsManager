<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gpnu.association.mapper.AssociationAndUserMapper">
	<insert id="add" parameterType="Map">
		insert into association_user (association_id, user_id, user_type, user_state, apply_comments, apply_time)
		values (#{associationId}, #{userId}, #{userType}, #{userState}, #{applyComments}, #{applyTime})
		<selectKey keyProperty="id" order="AFTER" resultType="int">
			SELECT LAST_INSERT_ID()
		</selectKey>
	</insert>
	
	<delete id="delete" parameterType="_int">
		delete from association_user where id = #{id}
	</delete>
	
	<select id="getSingle" parameterType="Map" resultType="Map">
		select * from association_user where id = #{id}
	</select>
	
	<select id="get" parameterType="Map" resultType="Map">
		SELECT
			au.*, 
			u.user_name,
			u.student_num,
			u.major,
			u.grade,
			a.name AS association_name,
			t.name AS type_name,
			CASE
		WHEN au.user_type = 1 THEN
			'社长'
		WHEN au.user_type = 2 THEN
			'副社长'
		WHEN au.user_type = 3 THEN
			'社员'
		END AS user_type_name,
		 CASE
		WHEN au.user_state = 0 THEN
			'待审核'
		WHEN au.user_state = 1 THEN
			'审核通过'
		WHEN au.user_state = 2 THEN
			'审核不通过'
		WHEN au.user_state = 3 THEN
			'申请退出'
		END AS user_state_name
		FROM
			association_user au
		INNER JOIN user u ON au.user_id = u.user_id
		LEFT JOIN association a ON au.association_id = a.association_id
		LEFT JOIN type t ON a.type_id = t.id
		<trim prefix="where" suffixOverrides="and">
			<if test="id != null">
				au.id = #{id} and
			</if>
			<if test="associationId != null">
				au.association_id = #{associationId} and
			</if>
			<if test="userType != null">
				au.user_type = #{userType} and
			</if>
			<if test="userState != null">
				au.user_state = #{userState} and
			</if>
		</trim>
		<if test="start!=null and pagesize!=null">  
       		 limit #{start},#{pagesize}  
   	    </if>  
	</select>
	
	<update id="update" parameterType="Map">
		update association_user 
		<trim prefix="set" suffixOverrides=",">
			<if test="userState != null">
				user_state=#{userState},
			</if>
			<if test="applyComments != null">
				apply_comments=#{applyComments},
			</if>
			<if test="applyTime != null">
				apply_time=#{applyTime},
			</if>
			<if test="checkPersonId != null">
				check_person_id=#{checkPersonId}, 
			</if>
			<if test="checkTime != null">
				check_time=#{checkTime}, 
			</if>
			<if test="checkComments != null">
				check_comments=#{checkComments}, 
			</if>
		</trim> 
		  where id=#{id}
	</update>
</mapper>